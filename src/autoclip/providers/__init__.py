"""External service providers (ASR / LLM / TTS).

Each subpackage exposes an abstract base class plus one or more concrete
implementations selected at runtime via Settings.

See design.md §6 (three-layer abstraction principle).
"""
